[project]
name = "lohas-reader"
version = "0.1.0"
description = "A Streamlit web-novel reader that scrapes chapters on demand."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "lxml>=6.1.1",
    "requests>=2.34.0",
    "streamlit>=1.60.0",
]

[tool.uv]
package = false
